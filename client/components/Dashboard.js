// IMPORTS ==================================================================//
import React from "react";

// DEFINE COMPONENT =========================================================//
const Dashboard = props => {
  return (
    <div>
      <p>You are loggen in!</p>
      <h4>Dashboard</h4>
    </div>
  );
};

// EXPORT ===================================================================//
export default Dashboard;
