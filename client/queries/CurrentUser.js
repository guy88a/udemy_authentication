// IMPORTS ==================================================================//
import gql from "graphql-tag";

// EXPORT ===================================================================//
export default gql`
  {
    user {
      id
      email
    }
  }
`;
